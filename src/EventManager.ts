"use strict";
import {EVENT} from './Event'
class EventManager{
    eventList: EVENT[];

    constructor() {
        this.eventList = <EVENT[]>[]
    }

    addEvent(targetEvent: EVENT) : EVENT{
        
    }
}